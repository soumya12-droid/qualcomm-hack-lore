# Phase 1 — indexing engine dependencies.
watchdog
pypdf
python-docx
python-pptx
openpyxl
onnxruntime
onnx

# onnxruntime-qnn only ships wheels for Windows ARM64 (the real Snapdragon X
# Copilot+ PC). It cannot be installed on other platforms, so it is left out
# here — install it separately on the target hardware:
#   pip install onnxruntime-qnn

# Phase 2 — backend API dependencies.
fastapi
uvicorn
