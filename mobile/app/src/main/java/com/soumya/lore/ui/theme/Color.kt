package com.soumya.lore.ui.theme

import androidx.compose.ui.graphics.Color

// Lore brand palette — calm, minimal, premium. Dark, ChatGPT-style surfaces.
// Emerald is intentionally used ONLY for primary actions/active states,
// never as a background or large surface, so the app doesn't "feel green".
val LoreBackground = Color(0xFF171717)
val LoreSurface = Color(0xFF262626)
val LoreTextPrimary = Color(0xFFECECEC)
val LoreTextSecondary = Color(0xFF9B9B9B)
val LoreEmerald = Color(0xFF10B981)
val LoreOutline = Color(0xFF3A3A3A)
